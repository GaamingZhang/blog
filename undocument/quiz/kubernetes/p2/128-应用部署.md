---
date: 2026-03-13
author: Gaaming Zhang
isOriginal: false
article: true
category:
  - Kubernetes
tag:
  - Kubernetes
  - 部署
  - Deployment
  - 应用部署
---

# 将应用程序部署到 Kubernetes

## 引言

将应用程序部署到 Kubernetes 是云原生转型的核心步骤。Kubernetes 提供了多种资源对象来管理应用的生命周期，包括 Deployment、Service、ConfigMap、Secret 等。理解这些资源的配置和使用方法，是成功部署应用的关键。

## 应用部署流程

### 部署流程概述

```
┌─────────────────────────────────────────────────────────────┐
│              应用部署流程                                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. 容器化应用                                               │
│     • 编写 Dockerfile                                       │
│     • 构建镜像                                              │
│     • 推送镜像仓库                                          │
│       │                                                      │
│       ▼                                                      │
│  2. 编写 Kubernetes 配置                                     │
│     • Deployment                                            │
│     • Service                                               │
│     • ConfigMap/Secret                                      │
│       │                                                      │
│       ▼                                                      │
│  3. 部署到集群                                               │
│     • kubectl apply                                         │
│     • Helm install                                          │
│       │                                                      │
│       ▼                                                      │
│  4. 验证部署                                                 │
│     • 检查 Pod 状态                                         │
│     • 检查 Service                                          │
│     • 测试访问                                              │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 容器化应用

### Dockerfile 示例

```dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install --production

COPY . .

EXPOSE 8080

USER node

CMD ["node", "server.js"]
```

### 构建和推送镜像

```bash
docker build -t my-registry.example.com/myapp:v1 .

docker push my-registry.example.com/myapp:v1
```

## Deployment 部署

### 基本 Deployment

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
  namespace: production
  labels:
    app: myapp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: my-registry.example.com/myapp:v1
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
```

### 完整 Deployment 配置

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
  namespace: production
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
        version: v1
    spec:
      serviceAccountName: myapp-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: myapp
        image: my-registry.example.com/myapp:v1
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: APP_ENV
          value: "production"
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: db_host
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: password
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: config
          mountPath: /etc/app
          readOnly: true
        - name: data
          mountPath: /var/data
      volumes:
      - name: config
        configMap:
          name: app-config
      - name: data
        persistentVolumeClaim:
          claimName: data-pvc
      imagePullSecrets:
      - name: registry-secret
```

## Service 配置

### ClusterIP Service

```yaml
apiVersion: v1
kind: Service
metadata:
  name: myapp
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: myapp
  ports:
  - port: 80
    targetPort: 8080
```

### NodePort Service

```yaml
apiVersion: v1
kind: Service
metadata:
  name: myapp-nodeport
spec:
  type: NodePort
  selector:
    app: myapp
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30080
```

### LoadBalancer Service

```yaml
apiVersion: v1
kind: Service
metadata:
  name: myapp-lb
spec:
  type: LoadBalancer
  selector:
    app: myapp
  ports:
  - port: 80
    targetPort: 8080
```

## Ingress 配置

### 基本 Ingress

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myapp-ingress
  namespace: production
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - myapp.example.com
    secretName: tls-secret
  rules:
  - host: myapp.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: myapp
            port:
              number: 80
```

## ConfigMap 和 Secret

### ConfigMap

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: production
data:
  db_host: "mysql-service"
  db_port: "3306"
  app.properties: |
    server.port=8080
    log.level=info
```

### Secret

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: production
type: Opaque
stringData:
  db_username: "admin"
  db_password: "S3cr3t!"
```

## 部署命令

### kubectl 部署

```bash
kubectl apply -f deployment.yaml

kubectl apply -f service.yaml

kubectl apply -f configmap.yaml

kubectl apply -f secret.yaml

kubectl apply -f ingress.yaml
```

### Helm 部署

```bash
helm install myapp ./chart -f values.yaml -n production

helm upgrade myapp ./chart -f values.yaml -n production
```

## 验证部署

### 检查部署状态

```bash
kubectl get deployments -n production

kubectl get pods -n production

kubectl get services -n production

kubectl get ingress -n production
```

### 查看详细信息

```bash
kubectl describe deployment myapp -n production

kubectl describe pod <pod-name> -n production

kubectl logs <pod-name> -n production

kubectl logs -f <pod-name> -n production
```

### 测试访问

```bash
kubectl port-forward svc/myapp 8080:80 -n production

curl http://localhost:8080/health
```

## 最佳实践

### 1. 使用命名空间隔离

```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: production
  labels:
    environment: production
```

### 2. 配置资源限制

```yaml
resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"
```

### 3. 配置健康检查

```yaml
livenessProbe:
  httpGet:
    path: /health
    port: 8080
readinessProbe:
  httpGet:
    path: /ready
    port: 8080
```

### 4. 使用版本标签

```yaml
metadata:
  labels:
    app: myapp
    version: v1
```

## 面试回答

**问题**: 如何将应用程序部署到 Kubernetes？

**回答**: 将应用程序部署到 Kubernetes 需要以下步骤：

**容器化应用**：编写 Dockerfile 定义容器镜像，使用 `docker build` 构建镜像，推送到镜像仓库。

**编写 Kubernetes 配置**：**Deployment** 定义应用的副本数、镜像、资源配置、健康检查等；**Service** 定义服务发现和负载均衡；**ConfigMap** 存储配置数据；**Secret** 存储敏感信息；**Ingress** 定义外部访问路由。

**部署到集群**：使用 `kubectl apply -f` 命令应用配置文件，或使用 Helm 进行包管理部署。

**验证部署**：使用 `kubectl get deployments/pods/services` 检查资源状态；使用 `kubectl describe` 查看详细信息；使用 `kubectl logs` 查看日志；使用 `kubectl port-forward` 测试访问。

**最佳实践**：使用命名空间隔离环境；配置资源请求和限制；配置 livenessProbe 和 readinessProbe 健康检查；使用 ConfigMap 和 Secret 管理配置和敏感信息；使用版本标签管理应用版本；配置滚动更新策略；使用 Ingress 暴露服务。
