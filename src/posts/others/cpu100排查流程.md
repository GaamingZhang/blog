---
date: 2026-01-12
author: Gaaming Zhang
isOriginal: false
article: true
category:
  - 运维
tag:
  - 运维
---

# 服务器CPU占用100%排查流程

## 1. 引言

服务器CPU占用率达到100%是生产环境中常见的紧急故障之一。这种情况会导致服务器响应缓慢、应用程序卡顿甚至崩溃，严重影响业务的正常运行。作为运维工程师或开发人员，快速定位并解决CPU高占用问题是一项必备技能。

本文将详细介绍服务器CPU占用100%的排查流程、常用工具、分析方法和解决方案，帮助读者系统掌握这一关键故障的处理能力。

## 2. CPU占用100%的危害

CPU占用率持续处于100%会对服务器和业务产生多方面的负面影响：

1. **系统响应缓慢**：所有进程都在争夺CPU资源，导致用户请求处理延迟增加
2. **应用程序崩溃**：关键服务可能因CPU饥饿而无法正常运行
3. **服务可用性下降**：负载均衡可能将流量转移到其他服务器，导致集群压力不均衡
4. **硬件损耗增加**：CPU长时间高负载运行会加速硬件老化
5. **数据丢失风险**：在极端情况下，系统可能出现不可预测的行为，导致数据损坏

## 3. 常用排查工具

### 3.1 系统监控工具

#### 3.1.1 top - 实时进程监控

```bash
top
```

- **主要功能**：实时显示系统进程的CPU、内存使用情况
- **关键指标**：
  - `%CPU`：进程CPU使用率
  - `%MEM`：进程内存使用率
  - `PID`：进程ID
  - `USER`：进程所有者
  - `COMMAND`：进程命令
- **交互操作**：
  - `P`：按CPU使用率排序
  - `M`：按内存使用率排序
  - `q`：退出

#### 3.1.2 htop - 增强版top工具

```bash
htop
```

- **主要功能**：top的增强版，提供更友好的界面
- **优势**：
  - 支持鼠标操作
  - 彩色显示不同类型的进程
  - 支持进程树视图

### 3.2 进程分析工具

#### 3.2.1 ps - 查看进程状态

```bash
ps aux | head -20  # 查看前20个进程
ps -eo pid,ppid,cmd,%cpu,%mem --sort=-%cpu | head -20  # 按CPU排序
```

#### 3.2.2 pidstat - 进程CPU使用统计

```bash
pidstat -u 1  # 每秒输出一次进程CPU使用情况
```

### 3.3 线程分析工具

#### 3.3.1 top -H - 查看线程信息

```bash
top -H -p <PID>  # 查看指定进程的线程CPU使用情况
```

#### 3.3.2 pthread_top - 线程监控工具

```bash
pthread_top -p <PID>  # 查看指定进程的线程详细信息
```

### 3.4 CPU性能分析工具

#### 3.4.1 perf - Linux性能分析工具

```bash
perf top -p <PID>  # 实时分析指定进程的CPU热点
perf record -p <PID>  # 记录指定进程的性能数据
perf report  # 分析性能记录文件
```

#### 3.4.2 gdb - 调试工具

```bash
gdb -p <PID>  # 调试指定进程
thread apply all bt  # 查看所有线程的调用栈
```

## 4. 详细排查流程

### 4.1 第一步：确认CPU高占用状态

首先，我们需要确认服务器的CPU确实处于高占用状态：

```bash
# 查看整体CPU使用情况
top -bn1 | grep "%Cpu"

# 查看每个CPU核心的使用情况
mpstat -P ALL 1
```

### 4.2 第二步：定位占用CPU最高的进程

使用top或ps命令找出占用CPU最高的进程：

```bash
# 使用top实时查看
top  # 然后按P键按CPU排序

# 使用ps命令查看
ps aux --sort=-%cpu | head -10
```

### 4.3 第三步：分析进程的线程情况

确定进程后，我们需要查看该进程内部的线程情况，找出占用CPU最高的线程：

```bash
# 查看指定进程的线程情况
top -H -p <PID>

# 查看线程的CPU使用统计
pidstat -p <PID> -t 1
```

### 4.4 第四步：定位线程的具体代码

对于占用CPU最高的线程，我们需要查看其执行的具体代码：

```bash
# 转换线程ID为十六进制（gdb需要十六进制格式）
printf "%x\n" <THREAD_ID>

# 使用perf分析CPU热点
perf top -p <PID>

# 使用gdb查看线程调用栈
gdb -p <PID>
# 在gdb中执行
thread <THREAD_ID_HEX>
bt
```

### 4.5 第五步：分析代码逻辑，定位问题根因

根据线程的调用栈信息，分析代码逻辑，找出导致CPU高占用的原因：

- **无限循环**：检查是否存在没有退出条件的循环
- **死循环**：检查循环条件是否始终为真
- **大量计算**：检查是否存在复杂的数学计算或递归调用
- **资源竞争**：检查是否存在大量的锁竞争或线程上下文切换
- **内存泄漏**：虽然内存泄漏主要影响内存，但严重的内存碎片可能导致CPU高占用

### 4.6 第六步：验证解决方案

针对定位到的问题，提出解决方案并验证：

1. **临时解决**：
   - 重启相关服务
   - 杀死占用CPU过高的进程
   - 增加服务器资源

2. **永久解决**：
   - 修复代码中的逻辑错误
   - 优化算法，减少计算复杂度
   - 改进并发设计，减少资源竞争
   - 增加系统资源，如升级CPU或增加服务器

3. **验证**：
   - 监控CPU使用率是否恢复正常
   - 测试应用程序功能是否正常
   - 观察系统性能指标是否稳定

## 5. 常见问题类型及解决方案

### 5.1 无限循环

**问题表现**：进程始终占用100% CPU，且不会自动恢复

**排查方法**：
```bash
perf top -p <PID>  # 查看CPU热点
```

**解决方案**：
- 修复代码中的循环条件
- 增加退出机制，如超时控制

**示例**：
```python
# 错误代码
while True:
    # 没有退出条件
    process_data()

# 正确代码
start_time = time.time()
timeout = 300  # 5分钟超时
while time.time() - start_time < timeout:
    if process_data():
        break
```

### 5.2 死锁

**问题表现**：多个进程或线程互相等待对方释放资源

**排查方法**：
```bash
# 使用pstack查看进程调用栈
pstack <PID>

# 使用gdb查看线程状态
gdb -p <PID>
thread apply all bt
```

**解决方案**：
- 统一锁的获取顺序
- 使用超时锁
- 减少锁的粒度
- 使用无锁数据结构

### 5.3 大量的上下文切换

**问题表现**：系统CPU使用率高，但单个进程的CPU使用率不高

**排查方法**：
```bash
# 查看上下文切换情况
vmstat 1
# cs列表示每秒上下文切换次数

# 查看每个进程的上下文切换情况
pidstat -w 1
```

**解决方案**：
- 减少线程数量
- 优化线程池大小
- 减少锁的使用频率
- 使用更高效的同步机制

### 5.4 大量IO操作

**问题表现**：进程CPU使用率高，同时IO等待时间长

**排查方法**：
```bash
# 查看IO使用情况
iostat -x 1

# 查看进程IO统计
pidstat -d 1
```

**解决方案**：
- 优化IO操作，减少读写次数
- 使用缓存机制
- 异步IO操作
- 升级存储设备

### 5.5 外部资源请求超时

**问题表现**：进程在等待外部资源时占用大量CPU

**排查方法**：
```bash
# 查看网络连接情况
netstat -antp | grep <PID>

# 查看文件描述符
lsof -p <PID>
```

**解决方案**：
- 增加请求超时时间
- 优化外部资源访问逻辑
- 增加重试机制
- 考虑使用缓存

## 6. 预防措施

### 6.1 监控系统

建立完善的监控系统，及时发现CPU高占用情况：

- 使用Prometheus + Grafana监控系统指标
- 设置CPU使用率告警阈值（如80%）
- 定期分析系统性能趋势

### 6.2 代码质量

提高代码质量，减少CPU高占用问题的发生：

- 进行代码审查，重点检查循环和并发逻辑
- 使用静态代码分析工具
- 进行性能测试，模拟高负载场景

### 6.3 系统优化

优化系统配置，提高CPU资源的利用效率：

- 调整内核参数，如线程调度策略
- 合理配置进程优先级
- 使用cgroups限制进程资源使用
- 定期清理无用进程和服务

### 6.4 容量规划

合理规划系统容量，避免CPU资源不足：

- 根据业务增长预测CPU需求
- 考虑使用自动扩展机制
- 定期评估系统负载情况

## 7. 常见问题

### 7.1 服务器CPU占用100%但没有明显的高占用进程，怎么办？

这种情况通常是由以下原因导致的：

1. **大量低优先级进程**：多个进程各占用少量CPU，但总和达到100%
   - 解决方案：使用`ps aux | awk '$3 > 1' | wc -l`查看高CPU进程数量，然后逐一分析

2. **内核线程占用**：某些内核线程（如kworker）可能占用大量CPU
   - 解决方案：使用`top -H`查看所有线程，包括内核线程，然后分析具体的内核线程

3. **硬件问题**：CPU硬件故障可能导致异常高占用
   - 解决方案：检查系统日志（`dmesg`），查看是否有硬件错误信息

### 7.2 如何快速定位Java应用的CPU高占用问题？

对于Java应用，可以使用以下工具和方法：

1. **使用jps查看Java进程**：
   ```bash
   jps
   ```

2. **使用top -H查看Java线程**：
   ```bash
   top -H -p <Java_PID>
   ```

3. **转换线程ID为十六进制**：
   ```bash
   printf "%x\n" <THREAD_ID>
   ```

4. **使用jstack查看线程栈**：
   ```bash
   jstack <Java_PID> | grep -A 20 <THREAD_ID_HEX>
   ```

5. **使用VisualVM或JProfiler进行分析**：这些工具提供更直观的图形界面，方便分析Java应用性能

### 7.3 CPU占用100%时，如何安全地重启服务？

在CPU高占用时重启服务需要谨慎，避免数据丢失：

1. **优雅关闭**：尽量使用服务的优雅关闭命令（如`systemctl stop service`）

2. **强制关闭**：如果优雅关闭失败，可以使用`kill -15 <PID>`（SIGTERM），如果仍然失败再使用`kill -9 <PID>`（SIGKILL）

3. **数据保护**：在关闭服务前，确保关键数据已保存

4. **监控重启过程**：重启后密切监控服务状态和CPU使用率

### 7.4 如何区分CPU密集型和IO密集型的高CPU占用？

1. **CPU密集型**：
   - 特点：CPU使用率高，IO等待时间低
   - 排查方法：使用`top`命令查看，`%wa`（IO等待）值较低
   - 常见原因：复杂计算、无限循环、死循环

2. **IO密集型**：
   - 特点：CPU使用率高，IO等待时间也高
   - 排查方法：使用`top`命令查看，`%wa`（IO等待）值较高
   - 常见原因：大量磁盘读写、网络请求超时、数据库查询慢

### 7.5 如何防止CPU占用100%导致的系统崩溃？

可以通过以下措施防止系统崩溃：

1. **设置进程资源限制**：使用`ulimit`或`cgroups`限制进程的CPU使用率

2. **启用OOM Killer**：确保系统的OOM Killer功能正常工作，可以在内存不足时杀死占用大量资源的进程

3. **使用看门狗机制**：实现监控脚本，当CPU使用率持续过高时自动重启关键服务

4. **部署高可用集群**：使用负载均衡和集群技术，确保单点故障不会影响整个系统

## 8. 总结

服务器CPU占用100%是一种常见但危害较大的故障。本文详细介绍了CPU高占用的排查流程，包括：

1. **确认问题**：使用top、mpstat等工具确认CPU高占用状态
2. **定位进程**：找出占用CPU最高的进程
3. **分析线程**：查看进程内部的线程情况
4. **定位代码**：使用perf、gdb等工具查看线程执行的具体代码
5. **解决问题**：根据分析结果修复代码或优化系统

同时，本文介绍了常见的CPU高占用问题类型、解决方案和预防措施，以及5个高频常见问题的解答。

掌握CPU高占用的排查和解决能力，对于保障服务器的稳定运行和业务的可用性至关重要。通过建立完善的监控系统、提高代码质量和系统优化，可以有效减少CPU高占用问题的发生，提高系统的稳定性和可靠性。

---

## 9. 实战案例分析

### 9.1 案例一：Java应用正则表达式回溯导致CPU飙升

**问题背景**：

生产环境电商订单服务突然出现CPU占用100%，导致订单处理延迟，用户投诉激增。

**排查过程**：

```bash
# 1. 确认问题
top -bn1 | head -5
# 输出：java进程CPU 198%（多核）

# 2. 定位高CPU线程
top -H -p 12345
# 输出：线程ID 12367 占用CPU 98%

# 3. 转换线程ID为十六进制
printf "%x\n" 12367
# 输出：303f

# 4. 获取线程栈
jstack 12345 | grep -A 30 "303f"
```

**线程栈信息**：

```
"order-processor-1" #15 prio=5 os_prio=0 tid=0x00007f8c84001800 nid=0x303f runnable [0x00007f8c6bffd000]
   java.lang.Thread.State: RUNNABLE
        at java.util.regex.Pattern$GroupHead.match(Pattern.java:4658)
        at java.util.regex.Pattern$Loop.match(Pattern.java:4785)
        at java.util.regex.Pattern$GroupTail.match(Pattern.java:4717)
        at java.util.regex.Pattern$BranchConn.match(Pattern.java:4568)
        at java.util.regex.Pattern$CharProperty.match(Pattern.java:3777)
        at java.util.regex.Pattern$Branch.match(Pattern.java:4604)
        at java.util.regex.Pattern$GroupHead.match(Pattern.java:4658)
        at java.util.regex.Pattern$Loop.match(Pattern.java:4785)
        ...
        at com.example.OrderService.validateEmail(OrderService.java:156)
        at com.example.OrderService.processOrder(OrderService.java:89)
```

**问题定位**：

正则表达式验证邮箱时发生**灾难性回溯**。

**问题代码**：

```java
// 问题正则表达式
public boolean validateEmail(String email) {
    String pattern = "^([a-zA-Z0-9_\\-\\.]+)@((\\[[0-9]{1,3}\\.[0-9]{1,3}\\.[0-9]{1,3}\\.)|(([a-zA-Z0-9\\-]+\\.)+))([a-zA-Z]{2,4}|[0-9]{1,3})(\\]?)$";
    return email.matches(pattern);
}

// 当输入为 "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa!" 时
// 正则引擎会尝试大量回溯，导致CPU飙升
```

**解决方案**：

```java
// 方案1：使用更简单的正则表达式
public boolean validateEmail(String email) {
    String pattern = "^[A-Za-z0-9+_.-]+@[A-Za-z0-9.-]+$";
    return email.matches(pattern);
}

// 方案2：限制输入长度
public boolean validateEmail(String email) {
    if (email == null || email.length() > 100) {
        return false;
    }
    String pattern = "^[A-Za-z0-9+_.-]+@[A-Za-z0-9.-]+$";
    return email.matches(pattern);
}

// 方案3：使用预编译正则
private static final Pattern EMAIL_PATTERN = 
    Pattern.compile("^[A-Za-z0-9+_.-]+@[A-Za-z0-9.-]+$");

public boolean validateEmail(String email) {
    if (email == null || email.length() > 100) {
        return false;
    }
    return EMAIL_PATTERN.matcher(email).matches();
}
```

**经验总结**：
- 避免使用复杂的正则表达式
- 对用户输入进行长度限制
- 使用预编译正则提高性能
- 监控正则表达式执行时间

### 9.2 案例二：Python死循环导致CPU 100%

**问题背景**：

数据处理脚本运行一段时间后CPU持续100%，无法正常完成。

**排查过程**：

```bash
# 1. 定位进程
ps aux --sort=-%cpu | head -5
# 输出：python data_processor.py CPU 99%

# 2. 使用py-spy分析
py-spy top --pid 23456
```

**py-spy输出**：

```
Collecting samples from 'python data_processor.py' (pid 23456)
Total Samples 100
GIL: 100.00%, Active: 100.00%, Threads: 1

  %Own   %Total  OwnTime  TotalTime  Function (filename:line)
 100.00%  100.00%   10.0s     10.0s   process_batch (data_processor.py:45)
   0.00%  100.00%   0.0s      10.0s   main (data_processor.py:78)
```

**问题代码**：

```python
# data_processor.py
def process_batch(data):
    results = []
    i = 0
    while i < len(data):
        item = data[i]
        if item['status'] == 'skip':
            continue  # Bug: 没有增加i，导致死循环
        results.append(process_item(item))
        i += 1
    return results
```

**解决方案**：

```python
# 方案1：修复循环逻辑
def process_batch(data):
    results = []
    i = 0
    while i < len(data):
        item = data[i]
        if item['status'] == 'skip':
            i += 1  # 修复：增加计数器
            continue
        results.append(process_item(item))
        i += 1
    return results

# 方案2：使用for循环（更安全）
def process_batch(data):
    results = []
    for item in data:
        if item['status'] == 'skip':
            continue
        results.append(process_item(item))
    return results

# 方案3：添加超时保护
import signal

def timeout_handler(signum, frame):
    raise TimeoutError("Processing timeout")

def process_batch_with_timeout(data, timeout=300):
    signal.signal(signal.SIGALRM, timeout_handler)
    signal.alarm(timeout)
    try:
        return process_batch(data)
    finally:
        signal.alarm(0)
```

### 9.3 案例三：Go程序Goroutine泄漏

**问题背景**：

Go微服务运行一段时间后CPU和内存持续增长，最终导致服务不可用。

**排查过程**：

```bash
# 1. 查看进程状态
top -H -p 34567
# 发现大量线程

# 2. 使用pprof分析
curl http://localhost:6060/debug/pprof/goroutine?debug=1 > goroutine.txt

# 3. 查看goroutine数量
curl http://localhost:6060/debug/pprof/goroutine?debug=1 | grep "goroutine profile:"
# 输出：goroutine profile: total 50000
```

**问题代码**：

```go
// 问题代码：goroutine泄漏
func processRequests(requests <-chan Request) {
    for req := range requests {
        go func() {
            // 启动goroutine但没有退出机制
            for {
                time.Sleep(1 * time.Second)
                // 永远运行，导致goroutine泄漏
            }
        }()
        handleRequest(req)
    }
}
```

**解决方案**：

```go
// 方案1：使用context控制goroutine生命周期
func processRequests(ctx context.Context, requests <-chan Request) {
    for req := range requests {
        go func(r Request) {
            ticker := time.NewTicker(1 * time.Second)
            defer ticker.Stop()
            for {
                select {
                case <-ctx.Done():
                    return
                case <-ticker.C:
                    // 执行周期任务
                }
            }
        }(req)
        handleRequest(req)
    }
}

// 方案2：使用worker pool
func processRequests(requests <-chan Request, workers int) {
    var wg sync.WaitGroup
    
    for i := 0; i < workers; i++ {
        wg.Add(1)
        go func() {
            defer wg.Done()
            for req := range requests {
                handleRequest(req)
            }
        }()
    }
    
    wg.Wait()
}

// 方案3：添加监控
func monitorGoroutines() {
    ticker := time.NewTicker(30 * time.Second)
    for range ticker.C {
        count := runtime.NumGoroutine()
        log.Printf("Current goroutines: %d", count)
        if count > 1000 {
            log.Printf("WARNING: Too many goroutines!")
        }
    }
}
```

### 9.4 案例四：Node.js事件循环阻塞

**问题背景**：

Node.js API服务响应变慢，CPU占用100%。

**排查过程**：

```bash
# 1. 使用clinic.js分析
npm install -g clinic
clinic doctor -- node app.js

# 2. 查看CPU profile
node --prof app.js
node --prof-process isolate-*.log > profile.txt
```

**问题代码**：

```javascript
// 问题代码：同步计算阻塞事件循环
app.get('/calculate', (req, res) => {
    const data = req.body.data;
    
    // 同步执行大量计算，阻塞事件循环
    const result = data.map(item => {
        return heavyCalculation(item);
    });
    
    res.json(result);
});

function heavyCalculation(item) {
    // 模拟复杂计算
    let result = 0;
    for (let i = 0; i < 10000000; i++) {
        result += Math.sqrt(item * i);
    }
    return result;
}
```

**解决方案**：

```javascript
// 方案1：使用worker_threads
const { Worker, isMainThread, parentPort, workerData } = require('worker_threads');

app.get('/calculate', async (req, res) => {
    const data = req.body.data;
    
    const results = await Promise.all(data.map(item => {
        return new Promise((resolve, reject) => {
            const worker = new Worker('./calculator.js', {
                workerData: item
            });
            worker.on('message', resolve);
            worker.on('error', reject);
            worker.on('exit', (code) => {
                if (code !== 0) reject(new Error(`Worker stopped with exit code ${code}`));
            });
        });
    }));
    
    res.json(results);
});

// calculator.js
const { parentPort, workerData } = require('worker_threads');

function heavyCalculation(item) {
    let result = 0;
    for (let i = 0; i < 10000000; i++) {
        result += Math.sqrt(item * i);
    }
    return result;
}

parentPort.postMessage(heavyCalculation(workerData));

// 方案2：拆分任务，让出事件循环
async function processInBatches(data, batchSize = 10) {
    const results = [];
    for (let i = 0; i < data.length; i += batchSize) {
        const batch = data.slice(i, i + batchSize);
        results.push(...batch.map(heavyCalculation));
        await new Promise(resolve => setImmediate(resolve));
    }
    return results;
}
```

### 9.5 案例五：MySQL慢查询导致数据库服务器CPU飙升

**问题背景**：

数据库服务器CPU持续100%，应用响应缓慢。

**排查过程**：

```bash
# 1. 登录MySQL查看进程
mysql -e "SHOW PROCESSLIST;"

# 2. 查看慢查询日志
mysqldumpslow -s t /var/log/mysql/slow.log | head -20

# 3. 使用pt-query-digest分析
pt-query-digest /var/log/mysql/slow.log > slow_report.txt
```

**问题SQL**：

```sql
-- 问题查询：全表扫描
SELECT * FROM orders 
WHERE YEAR(created_at) = 2024 
  AND MONTH(created_at) = 1
ORDER BY created_at DESC;

-- EXPLAIN结果
-- type: ALL (全表扫描)
-- rows: 5000000
-- Extra: Using where; Using filesort
```

**解决方案**：

```sql
-- 方案1：优化查询条件，使用索引
SELECT * FROM orders 
WHERE created_at >= '2024-01-01' 
  AND created_at < '2024-02-01'
ORDER BY created_at DESC;

-- 方案2：添加复合索引
CREATE INDEX idx_created_at ON orders(created_at);

-- 方案3：分页查询
SELECT * FROM orders 
WHERE created_at >= '2024-01-01' 
  AND created_at < '2024-02-01'
ORDER BY created_at DESC
LIMIT 100 OFFSET 0;

-- 方案4：使用覆盖索引
SELECT id, order_no, created_at FROM orders 
WHERE created_at >= '2024-01-01' 
  AND created_at < '2024-02-01'
ORDER BY created_at DESC;
```

---

## 10. 语言特定排查工具

### 10.1 Java排查工具

#### 10.1.1 JDK自带工具

```bash
# jps - 查看Java进程
jps -lvm

# jstat - 查看GC情况
jstat -gc <pid> 1000

# jmap - 内存分析
jmap -heap <pid>
jmap -histo <pid> | head -20
jmap -dump:format=b,file=heap.hprof <pid>

# jstack - 线程栈分析
jstack <pid> > thread_dump.txt

# jinfo - 查看JVM参数
jinfo -flags <pid>
```

#### 10.1.2 Arthas - 阿里开源诊断工具

```bash
# 安装
curl -O https://arthas.aliyun.com/arthas-boot.jar
java -jar arthas-boot.jar

# 常用命令
dashboard          # 实时面板
thread             # 线程信息
thread -n 5        # CPU占用最高的5个线程
thread <thread-id> # 查看指定线程栈
jad <class>        # 反编译类
watch <class> <method> '{params,returnObj}'  # 方法监控
trace <class> <method>  # 方法调用链追踪
profiler start     # CPU火焰图
profiler stop
```

#### 10.1.3 Java Flight Recorder (JFR)

```bash
# 启动JFR
jcmd <pid> JFR.start name=profile duration=60s filename=profile.jfr

# 分析JFR文件
# 使用JDK Mission Control (JMC) 打开 profile.jfr
```

### 10.2 Python排查工具

#### 10.2.1 py-spy - 采样分析器

```bash
# 安装
pip install py-spy

# 实时查看
py-spy top --pid <pid>

# 生成火焰图
py-spy record -o flamegraph.svg --pid <pid>

# dump调用栈
py-spy dump --pid <pid>
```

#### 10.2.2 cProfile - 内置分析器

```python
import cProfile
import pstats

# 分析代码
profiler = cProfile.Profile()
profiler.enable()

# 你的代码
main()

profiler.disable()
stats = pstats.Stats(profiler)
stats.sort_stats('cumulative')
stats.print_stats(20)
```

#### 10.2.3 memory_profiler - 内存分析

```bash
pip install memory_profiler

# 使用装饰器
from memory_profiler import profile

@profile
def my_func():
    # 你的代码
    pass
```

### 10.3 Go排查工具

#### 10.3.1 pprof - 性能分析

```go
// 在代码中启用pprof
import (
    "net/http"
    _ "net/http/pprof"
)

func main() {
    go func() {
        http.ListenAndServe("localhost:6060", nil)
    }()
    // ...
}
```

```bash
# 查看CPU profile
go tool pprof http://localhost:6060/debug/pprof/profile?seconds=30

# 查看goroutine
go tool pprof http://localhost:6060/debug/pprof/goroutine

# 查看heap
go tool pprof http://localhost:6060/debug/pprof/heap

# 交互式分析
(pprof) top10
(pprof) list <function>
(pprof) web  # 生成火焰图
```

#### 10.3.2 trace - 执行追踪

```go
import (
    "runtime/trace"
    "os"
)

func main() {
    f, _ := os.Create("trace.out")
    defer f.Close()
    trace.Start(f)
    defer trace.Stop()
    
    // 你的代码
}
```

```bash
# 分析trace
go tool trace trace.out
```

### 10.4 Node.js排查工具

#### 10.4.1 clinic.js - 性能诊断套件

```bash
# 安装
npm install -g clinic

# Doctor - 自动诊断
clinic doctor -- node app.js

# Flame - 火焰图
clinic flame -- node app.js

# Bubbleprof - 异步分析
clinic bubbleprof -- node app.js
```

#### 10.4.2 v8-profiler

```javascript
const profiler = require('v8-profiler-next');
const fs = require('fs');

// 开始CPU分析
profiler.startProfiling('CPU profile');

// ... 你的代码 ...

// 停止并保存
const profile = profiler.stopProfiling('CPU profile');
profile.export(function(error, result) {
    fs.writeFileSync('profile.cpuprofile', result);
    profile.delete();
});
```

#### 10.4.3 Node.js内置分析

```bash
# 生成CPU profile
node --prof app.js

# 处理profile
node --prof-process isolate-*.log > processed.txt

# 使用Chrome DevTools分析
node --inspect app.js
# 然后在Chrome中打开 chrome://inspect
```

### 10.5 通用排查工具

#### 10.5.1 perf - Linux性能分析

```bash
# 实时查看热点
perf top

# 分析特定进程
perf top -p <pid>

# 记录性能数据
perf record -g -p <pid> -- sleep 30

# 查看报告
perf report

# 生成火焰图
perf script | stackcollapse-perf.pl | flamegraph.pl > flame.svg
```

#### 10.5.2 火焰图工具

```bash
# 安装FlameGraph
git clone https://github.com/brendangregg/FlameGraph

# 生成火焰图
perf script | ./FlameGraph/stackcollapse-perf.pl | ./FlameGraph/flamegraph.pl > flame.svg
```

#### 10.5.3 bpftrace - eBPF追踪

```bash
# 安装
# Ubuntu: apt install bpftrace

# 追踪CPU高的函数
bpftrace -e 'profile:hz:99 /pid == <pid>/ { @[ustack] = count(); }'

# 追踪系统调用
bpftrace -e 'tracepoint:syscalls:sys_enter_* /pid == <pid>/ { @[probe] = count(); }'
```

---

## 11. 自动化排查脚本

### 11.1 一键排查脚本

```bash
#!/bin/bash
# cpu_analyze.sh - CPU高占用一键排查脚本

PID=$1
OUTPUT_DIR="/tmp/cpu_analysis_$(date +%Y%m%d_%H%M%S)"

if [ -z "$PID" ]; then
    echo "Usage: $0 <PID>"
    exit 1
fi

mkdir -p $OUTPUT_DIR

echo "=== CPU Analysis for PID $PID ==="
echo "Output directory: $OUTPUT_DIR"

# 1. 基本信息
echo "[1/8] Collecting basic info..."
ps -p $PID -o pid,ppid,user,%cpu,%mem,vsz,rss,stat,start,time,command > $OUTPUT_DIR/process_info.txt
cat /proc/$PID/status > $OUTPUT_DIR/process_status.txt
cat /proc/$PID/limits > $OUTPUT_DIR/process_limits.txt

# 2. 线程信息
echo "[2/8] Collecting thread info..."
top -H -b -n 1 -p $PID > $OUTPUT_DIR/threads.txt

# 3. 系统调用
echo "[3/8] Collecting syscalls..."
strace -c -p $PID -o $OUTPUT_DIR/strace_summary.txt &
STRACE_PID=$!
sleep 10
kill $STRACE_PID 2>/dev/null

# 4. 网络连接
echo "[4/8] Collecting network info..."
lsof -p $PID > $OUTPUT_DIR/open_files.txt
netstat -antp | grep $PID > $OUTPUT_DIR/network_connections.txt

# 5. perf分析
echo "[5/8] Running perf analysis..."
perf record -g -p $PID -o $OUTPUT_DIR/perf.data -- sleep 10 2>/dev/null
perf report -i $OUTPUT_DIR/perf.data --stdio > $OUTPUT_DIR/perf_report.txt 2>/dev/null

# 6. 检测语言类型并执行特定分析
CMD=$(ps -p $PID -o command= | head -1)

if [[ $CMD == *"java"* ]]; then
    echo "[6/8] Detected Java process, running Java-specific analysis..."
    jstack $PID > $OUTPUT_DIR/jstack.txt 2>/dev/null
    jmap -heap $PID > $OUTPUT_DIR/jmap_heap.txt 2>/dev/null
    jstat -gc $PID > $OUTPUT_DIR/jstat_gc.txt 2>/dev/null
elif [[ $CMD == *"python"* ]]; then
    echo "[6/8] Detected Python process..."
    py-spy dump --pid $PID > $OUTPUT_DIR/pyspy_dump.txt 2>/dev/null
elif [[ $CMD == *"node"* ]]; then
    echo "[6/8] Detected Node.js process..."
    curl -s http://localhost:9229/json > $OUTPUT_DIR/node_debug.json 2>/dev/null
else
    echo "[6/8] Unknown process type, skipping language-specific analysis"
fi

# 7. 系统状态
echo "[7/8] Collecting system status..."
vmstat 1 5 > $OUTPUT_DIR/vmstat.txt
iostat -x 1 5 > $OUTPUT_DIR/iostat.txt
mpstat -P ALL 1 5 > $OUTPUT_DIR/mpstat.txt

# 8. 生成报告
echo "[8/8] Generating report..."
REPORT=$OUTPUT_DIR/analysis_report.txt
echo "=== CPU Analysis Report ===" > $REPORT
echo "Time: $(date)" >> $REPORT
echo "PID: $PID" >> $REPORT
echo "" >> $REPORT

echo "=== Top CPU Threads ===" >> $REPORT
head -20 $OUTPUT_DIR/threads.txt >> $REPORT
echo "" >> $REPORT

echo "=== Process Info ===" >> $REPORT
cat $OUTPUT_DIR/process_info.txt >> $REPORT
echo "" >> $REPORT

if [ -f $OUTPUT_DIR/perf_report.txt ]; then
    echo "=== Perf Hotspots ===" >> $REPORT
    head -30 $OUTPUT_DIR/perf_report.txt >> $REPORT
fi

echo ""
echo "Analysis complete! Report saved to: $REPORT"
echo "Full data available in: $OUTPUT_DIR"
```

### 11.2 监控告警脚本

```bash
#!/bin/bash
# cpu_monitor.sh - CPU监控告警脚本

THRESHOLD=80
INTERVAL=60
LOG_FILE="/var/log/cpu_monitor.log"
ALERT_WEBHOOK="https://your-webhook-url"

log() {
    echo "$(date '+%Y-%m-%d %H:%M:%S') $1" >> $LOG_FILE
}

send_alert() {
    local message=$1
    curl -X POST -H 'Content-type: application/json' \
        --data "{\"text\":\"$message\"}" \
        $ALERT_WEBHOOK
}

while true; do
    # 获取CPU使用率最高的进程
    HIGH_CPU_PROCESSES=$(ps aux --sort=-%cpu | awk -v threshold=$THRESHOLD 'NR>1 && $3 > threshold {print $2, $3, $11}')
    
    if [ -n "$HIGH_CPU_PROCESSES" ]; then
        while read -r pid cpu cmd; do
            log "WARNING: Process $pid ($cmd) CPU usage: ${cpu}%"
            
            # 检查是否持续高CPU
            sleep 30
            CURRENT_CPU=$(ps -p $pid -o %cpu= | awk '{print int($1)}')
            
            if [ "$CURRENT_CPU" -gt "$THRESHOLD" ]; then
                ALERT_MSG="CPU Alert: Process $pid ($cmd) sustained high CPU: ${CURRENT_CPU}%"
                log "ALERT: $ALERT_MSG"
                send_alert "$ALERT_MSG"
            fi
        done <<< "$HIGH_CPU_PROCESSES"
    fi
    
    sleep $INTERVAL
done
```